# ==============================================
# Infrastructure Portfolio — Mathéo
# ==============================================
# Usage : docker compose up -d
# Secrets : dans le fichier .env (JAMAIS commité)
# ==============================================

services:

  # ---- Base de données ----
  db:
    image: mariadb:${MARIADB_VERSION}
    container_name: portfolio-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - backend
    # PAS de "ports:" ici — la DB n'est JAMAIS exposée
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # ---- WordPress (PHP-FPM) ----
  wordpress:
    image: wordpress:${WORDPRESS_VERSION}
    container_name: portfolio-wp
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'exec 3<>/dev/tcp/localhost/9000 && echo -e \"GET /ping HTTP/1.0\r\n\" >&3' 2>/dev/null || php -r 'exit(0);'"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    environment:
      WORDPRESS_DB_HOST: ${WORDPRESS_DB_HOST}
      WORDPRESS_DB_NAME: ${WORDPRESS_DB_NAME}
      WORDPRESS_DB_USER: ${WORDPRESS_DB_USER}
      WORDPRESS_DB_PASSWORD: ${WORDPRESS_DB_PASSWORD}
      # --- Sécurité : désactiver l'éditeur de fichiers WP ---
      WORDPRESS_CONFIG_EXTRA: "define('DISALLOW_FILE_EDIT', true);"
    volumes:
      - wordpress_data:/var/www/html
      - ./wp-content:/var/www/html/wp-content
      - ./php-custom/security.ini:/usr/local/etc/php/conf.d/security.ini:ro
    networks:
      - frontend
      - backend

  # ---- Nginx (Reverse Proxy) ----
  webserver:
    image: nginx:${NGINX_VERSION}
    container_name: portfolio-nginx
    restart: unless-stopped
    depends_on:
      - wordpress
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - wordpress_data:/var/www/html:ro
      - ./wp-content:/var/www/html/wp-content:ro
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./certbot/conf:/etc/letsencrypt:ro
      - ./certbot/www:/var/www/certbot:ro
      - ./nginx/logs:/var/log/nginx
    networks:
      - frontend

  # ---- Certbot (SSL/TLS) ----
  certbot:
    image: certbot/certbot:v2.11.0
    container_name: portfolio-certbot
    volumes:
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot
    networks:
      - frontend

# ---- Réseaux isolés ----
networks:
  frontend:
    name: portfolio-frontend
  backend:
    name: portfolio-backend

# ---- Volumes persistants ----
volumes:
  db_data:
    name: portfolio-db-data
  wordpress_data:
    name: portfolio-wordpress-data
